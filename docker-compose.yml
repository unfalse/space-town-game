services:
  game:
    build: .
    environment:
      - PORT=3000
    ports:
      - "127.0.0.1:${HOST_PORT:-3000}:3000"
    volumes:
      - levels-data:/app/src/server
    restart: unless-stopped

volumes:
  levels-data:
